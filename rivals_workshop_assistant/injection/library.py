from .dependencies import InjectionLibrary


def read_injection_library():
    raise NotImplementedError
    # Get library path
    # If not exists, create and populate from a cdn I'll have to make.
    # Read files to text

    # Read text to injection_library: list of dependencies


def get_injection_library_from_gml(gml: str) -> InjectionLibrary:
    return []
